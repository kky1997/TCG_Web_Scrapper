version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"   # Express backend
      - "5173:5173"   # Vite frontend
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development